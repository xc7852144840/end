 function aaa123(){
      	  const nameElement = document.getElementById("username");
          const username = nameElement.value;
          if (username.length == 0) 
          { 
            alert("帳號不能為空"); 
          }

          const wordElement = document.getElementById("password");
          const password = wordElement.value;
          if (password.length == 0) 
          { 
            alert("密碼不能為空"); 
          }
 
      }

function aaa321(){
	const aElement = document.getElementById("name");
    const name = aElement.value;
const bElement = document.getElementById("tel");
    const tel = bElement.value;
const cElement = document.getElementById("addr");
    const addr = cElement.value;
    const dElement = document.getElementById("email");
    const email = dElement.value;
    const eElement = document.getElementById("username2");
    const username2 = eElement.value;
    const fElement = document.getElementById("password2");
          const password2 = fElement.value;

if(name.length == 0) {
alert("姓名不能為空"); 
}
if(tel.length == 0) {
alert("電話不能為空"); 
}
if(addr.length == 0) {
alert("住址不能為空"); 
}
if(email.length == 0) {
alert("信箱不能為空"); 
}
if (username2.length == 0) 
{ 
alert("帳號不能為空"); 
}
if(password2.length == 0) {
alert("密碼不能為空"); 
}



}

